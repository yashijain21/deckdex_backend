PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/<db-name>?retryWrites=true&w=majority
JWT_SECRET=replace-with-strong-jwt-secret

# Stripe (test mode)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxx
# Apple Pay / Google Pay are shown through "card" on supported devices/browsers.
# You can also enable other methods supported by your Stripe account.
STRIPE_PAYMENT_METHOD_TYPES=card,klarna,link

# Frontend URL used for Stripe success/cancel redirects
FRONTEND_URL=http://localhost:3000
